#ifndef FATAL_H
#define FATAL_H

#include <stdio.h>
#include <string.h>

namespace catu
{
    void errorf(char* type, ...)
    {
        for (int i = 0; i < strlen(type); i ++)
        {
            
        }
    }
}

#endif