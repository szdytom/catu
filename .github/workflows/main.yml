name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: remove VS codefile
      run: rm -rf .vscode
    - name: run test
      run: pwd
